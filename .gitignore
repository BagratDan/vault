# Internal design + planning docs and chat transcripts — not for the public repo.
# These contain raw Claude Code session JSONL, the rendered conversation, and
# the design specs / implementation plans that produced the build. The shipped
# code, README, MODEL_TRADEOFFS, and THREAT_MODEL are the canonical artifacts.
/docs/
/*.jsonl
/qvac-chat-transcript.md
/CLAUDE.md

# Node / pnpm
node_modules/
dist/
.pnpm-store/
*.tsbuildinfo

# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/

# Local data — created at runtime by `vault start`
/.data/
/.vault/

# Test artifacts
coverage/
playwright-report/
test-results/

# One-off spikes (findings live in docs/superpowers/notes/)
/spike/

# Env
.env
.env.local
.env.*.local
