{
  "name": "@vault/e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run --dir tests --exclude '**/e2e2-multipeer.spec.ts' --exclude '**/e2e3-consent.spec.ts'",
    "test:multipeer": "vitest run e2e2-multipeer.spec.ts",
    "test:consent": "vitest run e2e3-consent.spec.ts",
    "test:playwright": "playwright test"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/ws": "^8.5.12",
    "tsx": "^4.19.0",
    "vitest": "^2.1.0"
  }
}
